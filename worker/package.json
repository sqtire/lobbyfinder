{
  "name": "osu-mp-scanner-worker",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Always-on osu! multiplayer scanner: rolling sweep that indexes every lobby, matches the union of tournament pools, and serves tenant backfills + owner-only API walks.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx scripts/smoke.mts"
  },
  "dependencies": {
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}

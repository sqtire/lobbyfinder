{
  "name": "osu-mp-scanner-worker",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Always-on osu! multiplayer scanner: dual-front (live + rescan) lobby search against a live-editable beatmap pool.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
